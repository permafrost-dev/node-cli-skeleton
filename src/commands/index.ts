import { one } from '@/commands/one';
import { two } from '@/commands/two';
import { three } from '@/commands/three';

export const commands = [
    one,
    two,
    three,
];

export default commands;
